import MainPage from "./MainPage";
import SideBar from "./SideBar";

const Home = () => {
  return (
    <section className="wrapper">
      <SideBar />
      <MainPage />
    </section>
  );
};

export default Home;

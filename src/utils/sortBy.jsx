// eslint-disable-next-line react/prop-types
const sortBy = (savedBlogs, sortValue) => {
  console.log({ savedBlogs, sortValue });
  // "newest"
  // "most_liked"
  // "default"
  return "hello ";
};

export default sortBy;
